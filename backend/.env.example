# ===== Personal Decision Council 后端配置 =====
# 复制为 .env 并填写。后端会在启动时自动读取本文件（无需其它依赖）。
#
# LLM_PROVIDER 说明（三选一）：
#   留空（推荐）  → 有 DEEPSEEK_API_KEY 自动用 DeepSeek；无 key 自动用 Mock
#   deepseek      → 强制 DeepSeek（需填 key，否则回退 Mock）
#   mock          → 强制 Mock（调试/自测用，无需 key）

# 留空即可：填了下面的 key 就会自动启用 DeepSeek
LLM_PROVIDER=

# 填入你的 DeepSeek key（https://platform.deepseek.com/）
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat

# 本地 SQLite 文件路径
DB_PATH=council.db

# 服务监听
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info
